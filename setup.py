import setuptools

setuptools.setup(
    name='SSDCNet',
    version='1.0.0',
    url='https://github.com/unleashlive/SS-DCNet',
    packages=setuptools.find_packages(),
    include_package_data=True,
    install_requires=[],
)
